import React from "react";
import { IComponentDefinition } from "@common/types";

interface ILayerFieldsProps {
  layer: IComponentDefinition;
  isForPrint: boolean;
}

export const LayerDynamicFields: React.FC<ILayerFieldsProps> = () => {
  return <></>;
};
